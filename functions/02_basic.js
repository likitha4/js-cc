
function calculateCartPrice(val, val2, ...num1)
{
return num1;
}
console.log(calculateCartPrice(78,67,98,9867));

const user={
    name:"manish",
    price:97879
}
function handleObject(anyobj){
    console.log(`username is ${anyobj.name} and price is ${anyobj.price}`);

}
// handleObject(user)

handleObject({
    name:"kainaat",
    price:3456
})
